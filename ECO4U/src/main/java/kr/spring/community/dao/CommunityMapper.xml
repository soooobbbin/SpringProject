<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="kr.spring.community.dao.CommunityMapper">  
  	<select id="selectRowCount" parameterType="map"
  	                    resultType="integer">


		SELECT
		  COUNT(*)
		FROM community b JOIN member m
		ON b.mem_num = m.mem_num
		<where>
			<if test="keyword != null and keyword != ''">
				<if test="keyfield == 1">
					b.c_title LIKE '%' || #{keyword} || '%'
				</if>
				<if test="keyfield == 2">
					m.id LIKE '%' || #{keyword} || '%' 
				</if>
				<if test="keyfield == 3">
					b.c_content LIKE '%' || #{keyword} || '%'
				</if>
				<if test="keyfield == 4">
					b.c_title LIKE '%' || #{keyword} || '%' OR
					b.c_content LIKE '%' || #{keyword} || '%'
				</if>
			</if>
			<if test="c_category != null ">
				<if test="c_category == tip">
					c_category = 1
				</if>
				<if test="c_category == 함께해요">
					c_category = 2
				</if>
				<if test="c_category == 친환경 소식">
					c_category = 3
				</if>
			</if>
		</where>  	
	
</select>

	<select id="selectList" parameterType="map" resultType="communityVO">
		SELECT
  		   *
  		FROM (SELECT
  		        a.*,
  		        rownum rnum
  		      FROM (SELECT
  		              b.c_num,
  		              <![CDATA[
  		              REPLACE(REPLACE(b.c_title,'<','&lt;'),'>','&gt;') c_title,
  		              ]]>
					  b.c_category,
					  b.c_content,
					  b.c_hit,
					  b.c_filename,
					  b.reg_date,
					  b.modify_date,
					  m.id
					  
		            FROM community b JOIN member m
		            ON b.mem_num = m.mem_num
					<where>
						<if test="keyword != null and keyword != ''">
							<if test="keyfield == 1">
								b.c_title LIKE '%' || #{keyword} || '%'
							</if>
							<if test="keyfield == 2">
								m.id LIKE '%' || #{keyword} || '%'
							</if>
							<if test="keyfield == 3">
								b.c_content LIKE '%' || #{keyword} || '%'
							</if>
							<if test="keyfield == 4">
								b.c_title LIKE '%' || #{keyword} || '%' OR
								b.c_content LIKE '%' || #{keyword} || '%'
							</if>
						</if>
						<if test="c_category != null ">
							<if test="c_category == 1">
								c_category = 1
							</if>
							<if test="c_category == 2">
								c_category = 2
							</if>
							<if test="c_category == 3">
								c_category = 3
							</if>
						</if>
					</where>
					ORDER BY b.c_num DESC)a)
		<![CDATA[
  		WHERE rnum >= #{start} AND rnum <= #{end}
  		]]>
	</select>
	
	<!-- 글수정 -->
  	<update id="updateCommunity" parameterType="communityVO">
  		UPDATE community SET
  		    <if test="c_filename != ''">
  		    c_uploadfile=#{c_uploadfile},
  		    c_filename=#{c_filename},
  		    </if>
  		    c_category=#{c_category},
  			c_title=#{c_title},
  			c_content=#{c_content}
  		    modify_date=SYSDATE
  		WHERE c_num=#{c_num}
  	</update>
  	
  	
</mapper>