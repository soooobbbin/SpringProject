<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="kr.spring.faq.dao.QnAMapper">  
<select id="selectRowCount" parameterType="map" resultType="integer">
		SELECT
		  COUNT(*)
		FROM qna q JOIN member m
		ON q.mem_num = m.mem_num AND q.mem_num=#{mem_num}
		<where>
			<if test="category == 0">
				q.q_category = 1 OR q.q_category = 2 OR q.q_category = 3
			</if>
			<if test="category == 1">
				q.q_category = 1
			</if>
			<if test="category == 2">
				q.q_category = 2
			</if>
			<if test="category == 3">
				q.q_category = 3
			</if>
		</where>
  	</select>
  	<select id="selectQnAList" parameterType="map" resultType="qnAVO">
		SELECT 
		  *
		 FROM (SELECT
		 		a.*,
		 		rownum rnum
		 	   FROM (SELECT
		 	   			q.q_num,
		 	   			<![CDATA[
		 	   			REPLACE(REPLACE(q.q_title,'<','&lt;'),'>','&gt;') q_title,
		 	   			]]>
		 	   			q.q_content,
		 	   			q.reg_date,
		 	   			q.q_category,
		 	   			q.mem_num,
		 	   			q_photo,
		 	   			q_photo_name
		 	   		 FROM qna q JOIN member m
		 	   		 ON q.mem_num = m.mem_num AND q.mem_num=#{mem_num}
		 	   		 <where>
							<if test="category == 0">
								q.q_category = 1 OR q.q_category = 2 OR q.q_category = 3 
							</if>
							<if test="category == 1">
								q.q_category = 1
							</if>
							<if test="category == 2">
								q.q_category = 2
							</if>
							<if test="category == 3">
								q.q_category = 3
							</if>
					</where>
		 	   		 ORDER BY q.q_num DESC)a)
		 <![CDATA[
		 WHERE rnum >= #{start} AND rnum <= #{end}
		 ]]>
	</select>
	
 	<!-- 글수정 -->
 	<update id="updateQnA" parameterType="qnaVO">
 		UPDATE qna SET
 			<if test="q_photo_name != ''">
  		    q_photo=#{q_photo},
 			q_photo_name=#{q_photo_name},
  		    </if>
 			q_title=#{q_title},
 			q_content=#{q_content},
 			q_category=#{q_category},
 			modify_date=SYSDATE 
 		WHERE q_num=#{q_num}
 	</update>
  	
  	<!-- 댓글 목록 -->
  	<!-- <select id="selectListReply" parameterType="map" resultType="qnAcommentVO">
  		SELECT
  		   *
  		FROM (SELECT
  		        a.*,
  		        rownum rnum
  		      FROM (SELECT
  		              re_num,
  		              <![CDATA[
  		              REPLACE(REPLACE(re_content,'<','&lt;'),'>','&gt;') re_content,
  		              ]]>
  		              re_date,
  		              re_mdate,
  		              board_num,
  		              mem_num,
  		              id,
  		              nick_name
  		            FROM spboard_reply
  		            JOIN spmember USING(mem_num)
  		            WHERE board_num = #{board_num}
  		            ORDER BY re_num DESC)a)
  		<![CDATA[
  		WHERE rnum >= #{start} AND rnum <= #{end}
  		]]>
  	</select> -->
  	
</mapper>







